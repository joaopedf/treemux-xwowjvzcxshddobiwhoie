import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MedScope AI — Multi-Agent Clinical Reasoning",
  description:
    "AI-powered clinical decision support with multi-agent differential diagnosis, treatment planning, drug interaction checking, and evidence-based literature search.",
  keywords: [
    "clinical decision support",
    "AI healthcare",
    "differential diagnosis",
    "drug interactions",
    "medical AI",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
